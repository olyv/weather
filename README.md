# whether

1. To start java back-end locally run `docker compose up mosquitto`, run the app using `local` Spring profile (use [Intellij run configuration](message-listener/runConfigurations/App-local.run.xml))
2. Or use docker with `docker compose up -d`